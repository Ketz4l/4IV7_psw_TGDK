function validar(automovil){
    if(automovil.auto.value.length<100000)
    alert("Es costo del automovil no es valido");
        automovil.auto.focus();
        return false;
    if(automovil.auto.value.length>1000000)
    alert("Es costo del automovil no es valido");
        automovil.auto.focus();
        return false;
}

var checkOK = "1234567890";

    var checkStr = automovil.auto.value;

    var escorrecto = true;

var suminteres = (cuota*3.94/100);

    var interes = cuota+suminteres;
